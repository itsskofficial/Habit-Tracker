from turtle import title
import requests
import tkinter

window=tkinter.Tk()
window.config(h)
window.mainloop()
user_status=int(input("Press 0 for an existing user or 1 for a new one: "))
if user_status==0:
    print()

